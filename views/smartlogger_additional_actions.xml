<?xml version="1.0" encoding="utf-8"?>
<!-- ФАЙЛ: views/smartlogger_additional_actions.xml для Odoo 17 -->


<odoo>
    <!-- ========================================== -->
    <!-- ДОПОЛНИТЕЛЬНЫЕ ДЕЙСТВИЯ ДЛЯ SMARTLOGGER   -->
    <!-- ОПТИМИЗИРОВАНЫ ДЛЯ ODOO 17                -->
    <!-- ========================================== -->

    <!-- ==========================================
         ДЕЙСТВИЯ ДЛЯ ОКОН (ir.actions.act_window)
         ========================================== -->

    <!-- Kanban представление станций -->
    <record id="action_smartlogger_station_kanban" model="ir.actions.act_window">
        <field name="name">Станції SmartLogger (Kanban)</field>
        <field name="res_model">smartlogger.station</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="context">{'search_default_group_by_status': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Kanban представление ваших станцій SmartLogger (Odoo 17)
            </p>
            <p>
                Тут ви побачите всі станції, згруповані за статусами для зручного управління.
            </p>
            <p>
                <strong>🆕 Покращення в Odoo 17:</strong>
                Швидше завантаження, кращі візуальні ефекти та покращена інтерактивність!
            </p>
        </field>
    </record>

    <!-- Станции с ошибками -->
    <record id="action_smartlogger_error_stations" model="ir.actions.act_window">
        <field name="name">Станції з помилками</field>
        <field name="res_model">smartlogger.station</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('status', 'in', ['error', 'sync_error'])]</field>
        <field name="context">{'search_default_error_stations': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Станції з помилками відсутні!
            </p>
            <p>
                Це означає, що всі ваші станції працюють без помилок синхронізації.
            </p>
            <p>
                <strong>🆕 В Odoo 17:</strong> Покращена діагностика помилок та автоматичні поради для вирішення проблем.
            </p>
        </field>
    </record>

    <!-- Выборочная синхронизация -->
    <record id="action_manual_sync_wizard" model="ir.actions.act_window">
        <field name="name">Вибіркова синхронізація</field>
        <field name="res_model">smartlogger.sync.data.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_sync_mode': 'data_only'}</field>
    </record>

    <!-- Управление Cron заданиями -->
    <record id="action_smartlogger_cron_jobs" model="ir.actions.act_window">
        <field name="name">Автоматичні завдання SmartLogger</field>
        <field name="res_model">ir.cron</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('name', 'ilike', 'SmartLogger')]</field>
        <field name="context">{'create': False, 'delete': False}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Управління автоматичними завданнями SmartLogger (Odoo 17)
            </p>
            <p>Тут ви можете:</p>
            <ul>
                <li>Увімкнути/вимкнути автоматичну синхронізацію</li>
                <li>Змінити інтервали виконання</li>
                <li>Переглянути статус виконання завдань</li>
                <li>Налаштувати додаткові завдання обслуговування</li>
            </ul>
            <p>
                <strong>🆕 Нові можливості:</strong> Покращений інтерфейс управління Cron завданнями з детальною статистикою.
            </p>
        </field>
    </record>

    <!-- Расширенные настройки -->
    <record id="action_smartlogger_advanced_config" model="ir.actions.act_window">
        <field name="name">Розширені налаштування</field>
        <field name="res_model">ir.config_parameter</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[('key', 'ilike', 'huawei.fusionsolar%')]</field>
        <field name="context">{'create': True, 'delete': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Розширені налаштування SmartLogger (Odoo 17)
            </p>
            <p>
                <strong>Увага!</strong> Змінюйте ці параметри тільки якщо розумієте їх призначення.
            </p>
            <p>
                Тут можна додати нові параметри конфігурації або змінити існуючі.
            </p>
            <p>
                <strong>🆕 В Odoo 17:</strong> Покращений інтерфейс редагування з підказками та валідацією.
            </p>
        </field>
    </record>

    <!-- Очистка старых данных -->
    <record id="action_smartlogger_cleanup_data" model="ir.actions.act_window">
        <field name="name">Очищення старих даних</field>
        <field name="res_model">smartlogger.sync.data.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_sync_mode': 'data_only', 'cleanup_mode': True}</field>
    </record>

    <!-- Аналітика та звіти (нова функція для Odoo 17) -->
    <record id="action_smartlogger_analytics" model="ir.actions.act_window">
        <field name="name">Аналітика станцій</field>
        <field name="res_model">smartlogger.data</field>
        <field name="view_mode">graph,pivot,tree</field>
        <field name="context">{'search_default_this_month': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                🆕 Потужна аналітика станцій в Odoo 17
            </p>
            <p>
                Новий інструмент для глибокого аналізу продуктивності ваших сонячних станцій.
            </p>
            <ul>
                <li><strong>Графічний аналіз:</strong> Візуалізація трендів та паттернів</li>
                <li><strong>Зведені таблиці:</strong> Багатовимірний аналіз даних</li>
                <li><strong>Порівняння станцій:</strong> Ефективність між різними установками</li>
                <li><strong>Експорт звітів:</strong> Дані для зовнішнього аналізу</li>
            </ul>
        </field>
    </record>

    <!-- ==========================================
         СЕРВЕРНЫЕ ДЕЙСТВИЯ (ir.actions.server)
         ОПТИМИЗИРОВАНЫ ДЛЯ ODOO 17
         ========================================== -->

    <!-- Сброс блокировки API -->
    <record id="action_reset_api_block" model="ir.actions.server">
        <field name="name">Скинути блокування API</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="state">code</field>
        <field name="code">
# Сбрасываем блокировку API (оптимизировано для Odoo 17)
try:
    result = env['smartlogger.station'].reset_api_block()
    if result:
        # Используем новую систему уведомлений Odoo 17
        action = {
            'type': 'ir.actions.client',
            'tag': 'display_notification',
            'params': {
                'type': 'success',
                'title': 'Блокування API скинуто!',
                'message': 'Автоматичні завдання відновлено. Можете запускати синхронізацію.',
                'sticky': False,
                'fadeout': 5000,
            }
        }
        return action
    else:
        raise UserWarning('Блокування API скинуто.')
except Exception as e:
    raise UserError('Помилка скидання блокування: %s' % str(e))
        </field>
    </record>

    <!-- Проверка статуса API -->
    <record id="action_check_api_status" model="ir.actions.server">
        <field name="name">Перевірити статус API</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="state">code</field>
        <field name="code">
# Проверяем статус API и показываем информацию (Odoo 17)
IrConfigParameter = env['ir.config_parameter'].sudo()

api_blocked = IrConfigParameter.get_param('huawei.fusionsolar.api_blocked', 'false') == 'true'
auth_error_count = int(IrConfigParameter.get_param('huawei.fusionsolar.auth_error_count', '0'))
last_auth_error = IrConfigParameter.get_param('huawei.fusionsolar.last_auth_error', '')
last_auth_error_time = IrConfigParameter.get_param('huawei.fusionsolar.last_auth_error_time', '')
last_successful_sync = IrConfigParameter.get_param('huawei.fusionsolar.last_successful_sync', '')
block_reason = IrConfigParameter.get_param('huawei.fusionsolar.block_reason', '')

info_lines = []
info_lines.append('=== СТАТУС FUSIONSOLAR API (Odoo 17) ===')
info_lines.append('')

if api_blocked:
    info_lines.append('🔴 Статус API: ЗАБЛОКОВАНИЙ')
    info_lines.append('📋 Причина: %s' % (block_reason or 'Невідомо'))
    status_type = 'danger'
else:
    info_lines.append('🟢 Статус API: Активний')
    status_type = 'success'

info_lines.append('📊 Кількість помилок автентифікації: %d' % auth_error_count)

if last_auth_error:
    info_lines.append('❌ Остання помилка: %s' % last_auth_error)
    if last_auth_error_time:
        info_lines.append('⏰ Час останньої помилки: %s' % last_auth_error_time)

if last_successful_sync:
    info_lines.append('✅ Остання успішна синхронізація: %s' % last_successful_sync)

info_lines.append('')
if api_blocked:
    info_lines.append('💡 Для відновлення роботи:')
    info_lines.append('   1. Перевірте облікові дані API')
    info_lines.append('   2. Натисніть "Скинути блокування API"')
else:
    info_lines.append('💡 API працює нормально')

message = '\n'.join(info_lines)

# Используем улучшенную систему уведомлений Odoo 17
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'type': status_type,
        'title': 'Статус FusionSolar API',
        'message': message,
        'sticky': True,
        'className': 'o_notification_manager_message'
    }
}
return action
        </field>
    </record>

    <!-- Быстрая синхронизация всех станций -->
    <record id="server_action_quick_sync_all" model="ir.actions.server">
        <field name="name">Швидка синхронізація всіх станцій</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Запуск быстрой синхронизации для всех станций (Odoo 17)
try:
    result = env['smartlogger.station'].sync_fusionsolar_data()
    if result.get('success'):
        stations_count = result.get('stations_processed', 0)
        action = {
            'type': 'ir.actions.client',
            'tag': 'display_notification',
            'params': {
                'type': 'success',
                'title': 'Синхронізація завершена!',
                'message': f'Успішно оброблено {stations_count} станцій.',
                'sticky': False,
                'fadeout': 3000,
            }
        }
        return action
    else:
        raise UserError('Помилка синхронізації: %s' % result.get('message', 'Невідома помилка'))
except Exception as e:
    raise UserError('Помилка: %s' % str(e))
        </field>
    </record>

    <!-- Пометить станции как на обслуживании -->
    <record id="server_action_mark_maintenance" model="ir.actions.server">
        <field name="name">Позначити як "На обслуговуванні"</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Помечаем выбранные станции как на обслуживании (Odoo 17)
records.write({'status': 'maintenance'})

# Добавляем сообщение в chatter для каждой станции
for station in records:
    station.message_post(
        body=f'Станція переведена в режим обслуговування користувачем {env.user.name}',
        message_type='notification'
    )

action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'type': 'info',
        'title': 'Статус оновлено',
        'message': f'Вибрані станції ({len(records)}) позначено як "На обслуговуванні"',
        'sticky': False,
    }
}
return action
        </field>
    </record>

    <!-- Сброс ошибок станций -->
    <record id="server_action_reset_errors" model="ir.actions.server">
        <field name="name">Скинути помилки</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Сбрасываем ошибки станций с проблемами (Odoo 17)
error_stations = records.filtered(lambda r: r.status in ['error', 'sync_error'])
if error_stations:
    error_stations.write({
        'status': 'active',
        'last_error': False
    })

    # Добавляем сообщения в chatter
    for station in error_stations:
        station.message_post(
            body=f'Помилки скинуто користувачем {env.user.name}. Станція повернута в активний режим.',
            message_type='notification'
        )

    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'type': 'success',
            'title': 'Помилки скинуто',
            'message': f'Помилки скинуто для {len(error_stations)} станцій',
            'sticky': False,
        }
    }
    return action
else:
    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'type': 'warning',
            'title': 'Немає помилок',
            'message': 'Не знайдено станцій з помилками серед вибраних',
            'sticky': False,
        }
    }
    return action
        </field>
    </record>

    <!-- Простой экспорт информации о станциях (улучшенный для Odoo 17) -->
    <record id="server_action_show_station_info" model="ir.actions.server">
        <field name="name">Показати інформацію про станції</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Показываем информацию о выбранных станциях (Odoo 17)
info_lines = []
info_lines.append('=== ІНФОРМАЦІЯ ПРО ВИБРАНІ СТАНЦІЇ (Odoo 17) ===')
info_lines.append('')

total_capacity = 0
total_current_power = 0
total_daily_energy = 0

for station in records:
    info_lines.append('🏭 Станція: %s' % station.name)
    info_lines.append('  📋 Код: %s' % station.station_code)
    info_lines.append('  📊 Статус: %s' % station.status)
    info_lines.append('  ⚡ Потужність: %.2f кВт' % station.current_power)
    info_lines.append('  🔋 Добова енергія: %.2f кВт·год' % station.daily_energy)
    info_lines.append('  🎯 Ефективність: %.1f%%' % station.efficiency_percentage)
    info_lines.append('  🌐 Онлайн: %s' % ('Так' if station.is_online else 'Ні'))
    if station.last_sync:
        info_lines.append('  🔄 Остання синхронізація: %s' % station.last_sync)
    info_lines.append('')

    total_capacity += station.capacity
    total_current_power += station.current_power
    total_daily_energy += station.daily_energy

info_lines.append('📈 ЗАГАЛЬНА СТАТИСТИКА:')
info_lines.append('Загалом станцій: %d' % len(records))
info_lines.append('Загальна номінальна потужність: %.2f кВт' % total_capacity)
info_lines.append('Загальна поточна потужність: %.2f кВт' % total_current_power)
info_lines.append('Загальна добова енергія: %.2f кВт·год' % total_daily_energy)
if total_capacity > 0:
    total_efficiency = (total_current_power / total_capacity) * 100
    info_lines.append('Загальна ефективність: %.1f%%' % total_efficiency)

# Выводим информацию используя новую систему уведомлений Odoo 17
message = '\n'.join(info_lines)
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'type': 'info',
        'title': 'Інформація про станції',
        'message': message,
        'sticky': True,
        'className': 'o_notification_manager_message_info'
    }
}
return action
        </field>
    </record>

    <!-- Массовая установка приоритета синхронизации -->
    <record id="server_action_set_priority" model="ir.actions.server">
        <field name="name">Встановити високий пріоритет</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Устанавливаем высокий приоритет синхронизации (Odoo 17)
records.write({'sync_priority': 1})

# Добавляем сообщения в chatter
for station in records:
    station.message_post(
        body=f'Встановлено високий пріоритет синхронізації користувачем {env.user.name}',
        message_type='notification'
    )

action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'type': 'success',
        'title': 'Пріоритет оновлено',
        'message': f'Для {len(records)} станцій встановлено високий пріоритет синхронізації',
        'sticky': False,
    }
}
return action
        </field>
    </record>

    <!-- Принудительная синхронизация выбранных станций -->
    <record id="server_action_force_sync" model="ir.actions.server">
        <field name="name">Примусова синхронізація</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Принудительная синхронизация выбранных станций (Odoo 17)
try:
    # Обновляем время последней синхронизации и статус
    records.write({
        'last_sync': fields.Datetime.now(),
        'status': 'active'
    })

    # Добавляем сообщения в chatter
    for station in records:
        station.message_post(
            body=f'Примусова синхронізація виконана користувачем {env.user.name}',
            message_type='notification'
        )

    action = {
        'type': 'ir.actions.client',
        'tag': 'display_notification',
        'params': {
            'type': 'success',
            'title': 'Синхронізація виконана',
            'message': f'Примусова синхронізація виконана для {len(records)} станцій',
            'sticky': False,
        }
    }
    return action
except Exception as e:
    raise UserError('Помилка примусової синхронізації: %s' % str(e))
        </field>
    </record>

    <!-- Группировка станций по региону -->
    <record id="server_action_group_by_region" model="ir.actions.server">
        <field name="name">Групувати за регіонами</field>
        <field name="model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_model_id" ref="smartlogger.model_smartlogger_station"/>
        <field name="binding_view_types">list</field>
        <field name="state">code</field>
        <field name="code">
# Группируем станции по регионам (Odoo 17)
regions = {}
for station in records:
    region = station.region or 'Без регіону'
    if region not in regions:
        regions[region] = {
            'stations': [],
            'total_capacity': 0,
            'total_power': 0,
            'total_energy': 0
        }
    regions[region]['stations'].append(station.name)
    regions[region]['total_capacity'] += station.capacity
    regions[region]['total_power'] += station.current_power
    regions[region]['total_energy'] += station.daily_energy

info_lines = []
info_lines.append('=== ГРУПУВАННЯ ЗА РЕГІОНАМИ (Odoo 17) ===')
info_lines.append('')

for region, data in regions.items():
    stations = data['stations']
    info_lines.append('🌍 %s (%d станцій):' % (region, len(stations)))
    info_lines.append('  ⚡ Загальна потужність: %.2f кВт' % data['total_capacity'])
    info_lines.append('  🔋 Поточна потужність: %.2f кВт' % data['total_power'])
    info_lines.append('  📊 Добова енергія: %.2f кВт·год' % data['total_energy'])
    info_lines.append('  📋 Станції:')
    for name in stations:
        info_lines.append('    • %s' % name)
    info_lines.append('')

message = '\n'.join(info_lines)
action = {
    'type': 'ir.actions.client',
    'tag': 'display_notification',
    'params': {
        'type': 'info',
        'title': 'Групування за регіонами',
        'message': message,
        'sticky': True,
    }
}
return action
        </field>
    </record>

    <!-- ==========================================
         URL ДЕЙСТВИЯ (ir.actions.act_url)
         ========================================== -->

    <!-- Документация API -->
    <record id="action_smartlogger_api_docs" model="ir.actions.act_url">
        <field name="name">Документація Huawei FusionSolar API</field>
        <field name="url">https://forum.huawei.com/enterprise/en/communicate-with-fusionsolar-through-an-openapi-account/thread/591478-100027</field>
        <field name="target">new</field>
    </record>

    <!-- Новое: ссылка на Odoo 17 документацию -->
    <record id="action_odoo17_docs" model="ir.actions.act_url">
        <field name="name">Документація Odoo 17</field>
        <field name="url">https://www.odoo.com/documentation/17.0/</field>
        <field name="target">new</field>
    </record>

</odoo>